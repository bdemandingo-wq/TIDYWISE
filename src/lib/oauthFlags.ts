/**
 * OAuth provider feature flags.
 *
 * Shared between NativeSignupRedirect and LoginPage so the buttons
 * appear on both screens and are toggled from one place.
 */
export const APPLE_OAUTH_ENABLED = true;
export const GOOGLE_OAUTH_ENABLED = true;
